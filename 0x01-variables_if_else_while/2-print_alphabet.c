#include <stdio.h>
/**
 * main - lower case alphabet
 * Return: 0 (successful)
 */

int main(void)
{
	char abt;

	for (abt = 'a'; abt <= 'z'; abt++)
	{
		putchar(abt);
	}
	putchar('\n');
	return (0);
}
